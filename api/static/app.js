const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function resourceCard(label,value,percent,detail){const safe=Math.max(0,Math.min(100,Number(percent)||0));return `<article class="panel resource-card"><span>${esc(label)}</span><strong>${esc(value)}</strong><div class="progress"><i style="width:${safe}%"></i></div><small>${esc(detail)}</small></article>`}
function renderTimeline(points){const target=document.querySelector('#device-timeline');if(!points.length){target.innerHTML='<p class="training-note">O histórico horário aparecerá após as primeiras leituras do ambiente.</p>';return}const maxDevices=Math.max(...points.map(point=>Number(point.devices)||0),1);target.innerHTML=points.map(point=>{const height=Math.max(8,Math.round(((Number(point.devices)||0)/maxDevices)*100));const change=Number(point.change)||0;const changeClass=change>0?'up':change<0?'down':'stable';const changeText=change>0?`+${change}`:String(change);const hour=new Date(point.hour).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});return `<article class="timeline-column" title="${esc(point.devices)} dispositivos · ${esc(point.problems)} problemas · ${esc(point.readings)} leituras"><span class="change ${changeClass}">${esc(changeText)}</span><div><i style="height:${height}%"></i></div><strong>${esc(point.devices)}</strong><time>${esc(hour)}</time></article>`}).join('')}
(async()=>{const user=await sofia.initAuth();if(!user)return;document.querySelector('#role').textContent=user.role;const [h,m,d]=await Promise.all([sofia.api('/health'),sofia.api('/mcp/health'),sofia.api('/dashboard/summary')]);document.querySelector('#health').textContent=h.status;document.querySelector('#health').className='ok';document.querySelector('#mcp').textContent=m.status;document.querySelector('#mcp').className='ok';document.querySelector('#modules').textContent=(m.modules||[]).length;document.querySelector('#generated-at').textContent=`Atualizado ${new Date(d.generated_at).toLocaleString()}`;const r=d.resources||{},db=r.database||{};document.querySelector('#resource-dashboard').innerHTML=[resourceCard('CPU',`${r.cpu?.load_percent_estimate??0}%`,r.cpu?.load_percent_estimate,`${r.cpu?.cores??0} núcleos · carga 1m ${r.cpu?.load_1m??0}`),resourceCard('Memória',`${r.memory?.used_percent??0}%`,r.memory?.used_percent,`${r.memory?.used_gb??0} GB de ${r.memory?.total_gb??0} GB`),resourceCard('Disco',`${r.disk?.used_percent??0}%`,r.disk?.used_percent,`${r.disk?.used_gb??0} GB de ${r.disk?.total_gb??0} GB`),resourceCard('PostgreSQL',`${db.size_mb??0} MB`,db.connection_percent,`${db.connections??0} de ${db.max_connections??0} conexões`)].join('');document.querySelector('#service-grid').innerHTML=(d.services||[]).map(s=>`<article class="service-item"><i class="status-light ${esc(s.status)}"></i><span><strong>${esc(s.name)}</strong><small>${esc(s.status)}</small></span></article>`).join('');if(user.role==='admin')document.querySelector('#provider-grid').innerHTML=(d.providers||[]).map(p=>`<article class="panel provider-card"><span class="provider-state ${esc(p.status)}">${esc(p.status.replaceAll('_',' '))}</span><h3>${esc(p.name)}</h3><p>${esc(p.detail)}</p></article>`).join('');const training=d.training||{};document.querySelector('#training-count').textContent=training.cycles||0;document.querySelector('#knowledge-count').textContent=training.knowledge_updates||0;document.querySelector('#training-status').textContent=training.status_message||'Aguardando primeiro treinamento';document.querySelector('#training-light').className=training.status||'idle';document.querySelector('#training-time').textContent=training.last_training_at?`Última atividade: ${new Date(training.last_training_at).toLocaleString()}`:'Nenhuma atividade registrada';renderTimeline(d.device_timeline||[])})().catch(error=>{console.error(error);document.querySelector('main').insertAdjacentHTML('afterbegin',`<div class="panel error">Falha ao carregar painel: ${esc(error.message)}</div>`)})
