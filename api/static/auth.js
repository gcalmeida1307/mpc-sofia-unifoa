const sofia=(()=>{const token=localStorage.getItem('sofia-token');async function api(path,options={}){const headers={...(options.headers||{})};if(token)headers.Authorization=`Bearer ${token}`;const response=await fetch(path,{...options,headers});const data=await response.json().catch(()=>({}));if(response.status===401){localStorage.removeItem('sofia-token');if(!path.includes('/login'))location.href='/ui/login.html'}if(!response.ok)throw new Error(data.detail||'Falha na solicitação');return data}async function initAuth(){if(!token){location.href='/ui/login.html';return null}const user=await api('/auth/me');const profilePage=location.pathname.endsWith('/perfil.html');if(user.email_required&&!profilePage){location.href='/ui/perfil.html';return null}document.querySelector('#identity').textContent=`${user.display_name} · ${user.role}`;document.querySelectorAll('[data-admin]').forEach(x=>x.hidden=user.role!=='admin');const nav=document.querySelector('.topbar nav');if(nav&&!nav.querySelector('[data-profile-link]')){const profile=document.createElement('a');profile.href='/ui/perfil.html';profile.dataset.profileLink='';profile.textContent='Perfil';nav.append(profile);if(user.role==='admin'){const management=document.createElement('a');management.href='/ui/gestao.html';management.dataset.admin='';management.textContent='Gestão';nav.append(management)}}document.querySelector('#logout').onclick=async()=>{await api('/auth/logout',{method:'POST'});localStorage.removeItem('sofia-token');location.href='/ui/login.html'};return user}return{api,initAuth}})();