const message=document.querySelector('#message');
async function post(url,body){const response=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.detail||'Não foi possível concluir. Tente novamente.');return data}
document.querySelector('#login-form').onsubmit=async event=>{event.preventDefault();const button=event.submitter;message.className='auth-message';message.textContent='Validando acesso…';button.disabled=true;try{const data=await post('/auth/login',{username:document.querySelector('#username').value.trim(),password:document.querySelector('#password').value,otp:document.querySelector('#otp').value.replace(/\D/g,'')});localStorage.setItem('sofia-token',data.token);message.classList.add('success');message.textContent='Acesso confirmado. Abrindo a plataforma…';location.href='/ui/index.html'}catch(error){message.classList.add('error');message.textContent=error.message}finally{button.disabled=false}};
